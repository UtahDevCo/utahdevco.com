import { Header } from "@/components/header";

export default function Home() {
  return (
    <main className="flex flex-col min-h-screen max-w-viewport w-screen border-solid border-l-primary border-r-primary border-2 bg-primary">
      <Header />
    </main>
  );
}
